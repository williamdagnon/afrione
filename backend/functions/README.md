Supabase Edge Functions

Place your Edge Functions here and use `supabase functions deploy` to deploy.

Example:
- `purchase` function: validate user session, call `attempt_purchase` SQL function, return result.

I can scaffold an example Edge Function (TypeScript) if you want — tell me which flows you want in functions (purchase, notifications, admin tasks).